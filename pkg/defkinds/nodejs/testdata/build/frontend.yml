kind: nodejs
version: 12
frontend: true

sources:
  - src/

stages:
  dev:
    config_files:
      .env.dev: .env
  prod:
    config_files:
      .env.prod: .env

webserver:
  type: nginx
  config_file: nginx.conf
  healthcheck: true
